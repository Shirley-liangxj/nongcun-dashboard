# 农业农村局 内部数据看板与进度追踪平台
## 产品需求文档（PRD, Product Requirements Document）

> 本文件回答「**怎么做（How）**」——功能规格、交互、非功能要求、验收标准。
> 对应的分析文档见《需求分析文档.md》，负责回答「为什么做 + 做什么 + 为谁做」。
> PRD 是需求分析阶段的**产出物**，需求稳定后开**需求评审**，评审通过方可进入设计/开发。

| 项目 | 内容 |
|---|---|
| 文档状态 | 需求确认阶段（未评审） |
| 版本 | v0.2 |
| 撰写人 | 梁雪洁 |
| 创建日期 | 2026-09-02 |
| 适用范围 | 内部使用（生产环境含真实政务数据）；**本仓库为脱敏版，仅含文档与假数据演示，对外展示（作品集）必须脱敏，绝不存放真实数据** |

> **填写约定**：标注 【待画】 / 【待填写】 的章节由产品经理（梁雪洁）负责产出，是必须自己动手、面试要能讲清的部分。

---

## 0. 修订记录

| 版本 | 日期 | 修订人 | 修订说明 |
|---|---|---|---|
| v0.1 | 2026-09-02 | 梁雪洁 | 搭建 PRD 骨架，拆分自合并草稿 |
| v0.2 | 2026-09-03 | 梁雪洁 | 方案 A：M5 合并吸收进 M2（MVP 共 4 模块）；纠正"领导派任务"假设为"上级文件驱动、领导督促"；M2 内分常规进度/整改任务两类 |

---

## 1. 概述

本平台是面向区县级农业农村局的**内部数据看板与进度追踪平台**（G 端客户属性、内部效率工具形态）。核心价值（北极星 = 综合分析获取时延）见《需求分析文档.md》§1.2。

MVP 范围（Must）：数据导入、数据看板、AI 分析（双通道）、一键导出汇报。
优先级与边界详见《需求分析文档.md》§3 / §6。

---

## 2. 功能结构图（信息架构）

> 【待画】画出三层结构：首页 → 详情页 → 各功能入口。
> 用墨刀 / 纸笔 / 文字缩进均可，重点是层级关系，不是美观度。
> **这是考察你对系统结构的理解，必须由你本人完成，我不代画。**

建议层级（供你起笔，非定稿）：

```
首页（数据看板）
├ 各业务口进度概览卡片（名称/完成数/总数/进度条/百分比/异常标签）
├ 全局异常标记区（滞后优先排序）
├ AI 综合分析结论区（固定自动分析 + 对话式分析入口）
└ 导航
   ├ 进度管理（详情页：单业务口任务列表，含「常规进度」与「整改任务」两类，按年/镇筛选；整改任务由上级文件 AI 解析生成 → 时间线）
   ├ 数据导入（Excel 上传 + 校验）
   └ 一键导出汇报
```

---

## 3. 核心业务流程图（业务流转）

> 【待画】画出「人怎么用系统干活」的完整流程，含异常分支。重点标清：**谁（角色）、在何时、做什么动作、产出什么、异常怎么处理**。
> 与 §4 数据闭环图的区别：本节画「业务动作与人」，§4 画「数据怎么流动」。
> **必须由你本人完成（墨刀 / 纸笔 / 文字流程均可），我不代画。**

参考业务节点（供起笔）：

| 阶段 | 角色 | 动作 | 产出 | 异常分支 |
|---|---|---|---|---|
| 数据供给 | 业务口经办人 | 上传 Excel 台账 | 系统接收文件 | 格式错 → 返回错误行号，重传 |
| 数据治理 | 系统 + 管理员 | 校验 / Upsert 入库 | 结构化记录 | 行数骤降 → 疑似漏传预警 |
| 进度查看 | 领导 | 打开看板看全局 | 进度卡片 + 异常标签 | 发现滞后 → 触发催办 |
| 智能分析 | 领导 / 经办人 | 点生成 / 对话提问 | AI 结论（HITL 确认） | 结论存疑 → 人工编辑后确认 |
| 汇报输出 | 业务口经办人 | 一键导出汇报 Excel | 汇报材料 | — |

---

## 4. 数据闭环图（数据流）

> 【待画】画出一条完整数据链路，每个节点标注 **输入 → 输出 + 谁负责**：`Excel 上传 → 解析校验 → 入库 → 看板计算 → 展示 → AI 分析 → 导出`。
> **同样由你本人完成，我不代画。**

参考节点（供起笔）：

| 节点 | 输入 | 输出 | 负责方 |
|---|---|---|---|
| Excel 上传 | 业务口台账文件 | 文件入临时区 | 经办人 |
| 解析校验 | 文件 + 模板规则 | 错误行号清单 / 通过 | 系统 + 管理员兜底 |
| 入库（Upsert） | 校验通过数据 + 主键 | 更新或插入记录 | 系统 |
| 看板计算 | 全量数据 | 进度/异常/卡片 | 系统 |
| 展示 | 计算结果 | 首页卡片 | 系统 |
| AI 分析 | 聚合数据 + 意图 | 结论 / 对话回答 | 系统（HITL 确认） |
| 导出 | 聚合数据 | 汇报 Excel | 系统 |

---

## 5. 页面原型（墨刀线框）

> 【待画】画出核心界面的线框（低保真即可）：首页看板、进度管理详情页（含常规进度与整改任务两类视图）、AI 分析区（固定结论 + 对话输入框）、数据导入页、整改任务时间线（由上级文件 AI 解析生成）。
> 这是「实际页面长什么样」，与 §2 功能结构图（画能力分层）不同——**§2 按功能逻辑画，本节按页面画**。
> **由你本人在墨刀中绘制，我不代画；面试要能照图讲清每页放什么、为什么。**

---

## 6. 技术架构图

> 【待画】画出部署结构：前端（浏览器访问）→ 后端（Cursor 辅助开发）→ 数据库（SQLite）→ AI 服务（大模型 API，用于 M3 分析与 M5 解析）→ 部署（阿里云）。
> 标注内网环境约束（若内网锁 IE 内核，前端选型需调整——见需求分析文档 §5.6）。
> **由你本人完成，我不代画。**

---

## 7. 功能模块详细需求

### M1 数据看板（首页）

| 项 | 说明 |
|---|---|
| 功能描述 | 以卡片形式展示各业务口的进度概览 |
| 卡片要素 | 业务口名称 + 已完成数/总数 + 进度条 + 完成百分比 + 异常状态标签 |
| 示例 | `厕所改造 35/57 ████████░░░░ 61%` |
| 异常标签规则 | 当"完成率 < 时间进度 × 80%"时，自动标记"滞后"并高亮 |
| 排序 | 默认按"滞后优先、完成率升序"排列，让问题自动浮到最前 |
| 交互 | 点击卡片 → 进入该业务口详情页 |

### M2 进度管理（详情页，含两类任务）

| 项 | 说明 |
|---|---|
| 功能描述 | 展示单个业务口下的全部任务列表；**内部含两类**：① 常规进度（业务口主动报，软更新、无强制时限）；② 整改任务（上级文件 AI 解析生成，带时限 / 销号 / 问责，硬闭环） |
| 列表字段 | 任务名称 + 类型(常规/整改) + 当前阶段 + 进度 + 负责人 + 时限(整改类) |
| 筛选 | 支持按年份、按乡镇、按任务类型筛选（MVP 阶段必备） |
| 交互 | 支持点击表头排序；支持导出当前视图为 Excel（供汇报用） |
| 整改任务来源 | 上级文件（市局/巡察/督查）驱动，AI 解析 → 人工确认 → 生成任务卡；局领导是**督促/中转方**，非任务发起方 |

### M3 AI 分析

> **设计模式（双通道）**：本期确认采用「固定自动分析 + 对话式分析」双通道，二者共用同一份已聚合的跨镇跨年数据，向领导交付同一种价值（AI 综合结论）。

> | 通道 | 触发方式 | 输入 | 输出 |
> |---|---|---|---|
> | **通道 1：固定自动分析（推送式）** | 数据导入/更新后系统**自动生成**，或在看板/详情页底部点按钮生成，**不单独开页面** | 已聚合的全量数据 | 一段固定的结论性描述（覆盖各业务口进度、异常、跨年趋势） |
> | **通道 2：对话式分析（ChatBI / NLQ，拉取式）** | 领导在对话框输入自己想问、固定分析没覆盖的问题 | 领导的自由提问 + 已聚合数据 | 针对该问题的 AI 回答 |

> **术语：对话式分析 = ChatBI / NLQ（Natural Language Query，自然语言查询）**——用大白话问数据、系统用大白话答。本产品同时具备两种，符合行业主流。

| 项 | 说明 |
|---|---|
| 输出内容 | 基于已导入数据自动生成一段结论性描述；对话通道额外支持针对自由提问的专项回答 |
| 可编辑 | 用户可对结论进行编辑修正；对话回答同样可标注"有误"并修正 |
| **边界约束** | AI 仅做**描述性总结**，**不做追责性判断**；每条结论/回答须标注数据来源，最终口径以人工确认为准 |
| **对话通道强约束** | 对话回答必须**可溯源**：每条结论旁固定展示"引用了哪些业务口 / 哪年 / 哪些具体数值 + 数据截至时间"，禁止无来源的"编造式"回答 |

> ⚠️ 政务场景下 AI 结论必须具备**可溯源性**与**责任边界**。对话通道因问题开放，幻觉风险高于固定分析，故溯源要求更严——这是 HITL 在 M3 的延伸。

### M4 数据导入

| 项 | 说明 |
|---|---|
| 数据来源 | 各业务口近三年的存量 Excel 数据 |
| 推进策略 | **先做 1 个业务口试点，流程跑通后再铺开全部业务口** |
| 模板策略 | 【待确认】统一模板 vs 各自格式清洗（见需求分析文档 §5.2） |
| 校验规则 | 导入前校验：表头匹配、必填项非空、数值合法性、重复行检测 |
| 失败处理 | 校验不通过时，返回**具体错误行号 + 错误原因**，不静默丢弃 |
| **增量 vs 全量** | 采用 **全量 + Upsert**（见下） |

**增量 / 全量导入策略（PM 决策点）**

> 判断顺序：先问**使用者的能力**，再问**数据的形态**，最后才是技术方案。

| 方案 | 优点 | 缺点 | 适用 |
|---|---|---|---|
| 每次全量覆盖 | 用户无需判断哪些是新增；逻辑最简单 | 需传完整文件；历史被覆盖 | 数据量小、用户非专业 |
| 增量追加 | 文件小、速度快 | **要求用户准确判断新增范围，最容易出错**；无法同步历史数据的修改 | 数据量大且用户专业 |
| **全量 + Upsert** | 既能新增也能更新历史，用户零判断 | 需定义稳定的主键 | ✅ **推荐 MVP 采用** |

> **Upsert** = Update + Insert：按主键判断，库中已有则更新，没有则插入。
> **主键（Primary Key）** = 数据库给每一行数据发的"身份证号"——**唯一且不变**。Upsert 靠它认出"这一行我上次见没见过"。
> **主键设计（本项目建议稿）**：`业务口 + 乡镇 + 村 + 项目内唯一编号 + 年份`。以"厕所改造"为例：每行 = 某户某年的改厕记录，主键可取 `改厕|XX镇|YY村|户编号|2024`。主键必须**稳定且唯一**，否则 Upsert 失效。

**配套必须实现的 3 项（政务系统刚需）**

| # | 项 | 说明 |
|---|---|---|
| 1 | 导入批次留痕 | 记录 谁 / 何时 / 哪个文件 / 新增多少行 / 更新多少行 → 支持**按批次回滚** |
| 2 | 异常波动预警 | 本次导入行数比上次少 20% 以上时，提示"疑似漏传，请确认" |
| 3 | 覆盖前二次确认 | 展示"将覆盖 N 行、新增 M 行"，用户确认后才写入 |

### M5 任务管理 → 已合并吸收进 M2（MVP 方案 A）

> **决策（2026-09-03，方案 A）**：原独立模块 M5「任务管理」已合并吸收为 **M2 进度管理内的「整改任务」子类**。理由：① 整改任务来源是**上级文件（市局/巡察/督查）**，AI 解析生成，业务口照常报进度、领导督促跟踪——与常规进度的**执行端 / UI 高度重合**；② MVP 不过度设计，合并降复杂度；③ 仍保留「整改任务」为独立**数据实体**（带来源=上级文件、时限、销号状态），面试可讲清"为何没独立成模块"。若巡察整改常态化、多上级来源督办很重，后续迭代再独立为 M5。
>
> **原 M5 能力并入 M2 整改任务子类**：
> - 输入：上级下发的 PDF/Word 文件
> - AI 解析字段：任务名称、时间节点、责任业务口、具体要求
> - 人工确认：解析结果以**表单形式**展示，用户可编辑修正，确认后才创建任务卡片
> - 展示：在进度管理内按**时间线**排列，解决"不知道啥时候该干啥"
> - 维护：支持手动编辑/删除任务
>
> **来源澄清（重要）**：整改任务由**上级文件驱动**，局领导是**督促者/中转**，非任务发起方。原"领导基于巡查/上级文件派整改任务"措辞已纠正。

> **设计模式：HITL（Human-in-the-loop，人在回路）**——AI 先产出初稿 → 人审核 / 修正 → 确认后才生效。本项目中 **M3（AI 分析结论）与 M2 整改任务（AI 解析）两处均采用 HITL**，原因：政务场景不可接受 AI 直接输出未经过人工确认的结果（涉及责任归属）。

---

## 8. 非功能需求

| 类别 | 要求 | 说明 |
|---|---|---|
| 性能 | 首屏加载 ≤ 3 秒 | 内部系统，用户量小（数十人），不做过度设计 |
| 数据安全 | 数据留痕：记录谁在何时上传/修改了哪条数据 | 政务系统标配，与"领导评语不留痕"是两回事 |
| 权限 | MVP 采用透明型（全员可看），但**首页与详情页采用不同视图** | 领导看汇总+异常，经办人看本口明细 |
| 可用性 | 兼容主流浏览器 Chrome/Edge | 政务内网环境多为 IE 内核，需确认（见需求分析文档 §5.6） |
| 备份 | 数据库定期备份 | SQLite 文件级备份即可 |
| 数据准确率 | 系统不做二次计算，展示值以业务口提交的源数据为准 | 明确责任边界 |
| 可解释性 | 对话式分析（ChatBI）的每条回答必须可溯源：引用业务口 / 年份 / 具体数值 / 数据截至时间 | 政务追责场景刚需，防止 AI 编造无来源结论 |

---

## 9. 验收标准

> 将北极星指标拆成可量化的验收条件，项目上线时逐条核对（与第 5 节非功能需求互补）。

| 编号 | 验收项 | 标准 | 行业基准依据 |
|---|---|---|---|
| A1 | 首屏加载 | ≤ 3 秒 | Web 性能通用基准（Google 数据：加载 >3s 跳出率显著上升）；内部系统用户仅数十人，3s 宽松可达 |
| A2 | 数据更新后看板刷新延迟 | ≤ 5 分钟 | 本产品为**手动批量导入**（非实时流），刷新取决于"导入任务 + 重新聚合 + 缓存"；5 分钟兼顾可靠与体感 |
| A3 | 异常自动识别 | 滞后状态自动标红，无需人工比对 | 看板核心价值之一，替代"开会逐口对账" |
| A4 | 数据准确率 | 100%（展示值 = 业务口源数据，系统不做二次计算） | 责任边界：数据错属提交方，非系统算错 |
| A5 | AI 综合分析时延（对应北极星） | 固定分析（导入后自动生成）≤ 60 秒；对话式回答首字 ≤ 10 秒、完整回答 ≤ 30 秒 | LLM 应用通用体验基准；此条直接校验北极星"综合分析获取时延" |

> 注：A1 与第 5 节性能重复，此处从"验收核对"视角再列一次。A2 的 5 分钟是**手动导入场景**的合理 SLA——若是实时接口同步，应压到秒级，本项目不涉及。

---

## 10. 异常与边界场景

| # | 场景 | 期望处理 |
|---|---|---|
| E1 | Excel 表头与模板不匹配 | 提示具体行/列，拒绝导入，给出模板下载入口 |
| E2 | 同一批次数据重复导入 | 检测到重复后提示"覆盖/跳过/取消" |
| E3 | AI 解析 PDF 得到乱码或字段缺失 | 表单留空并标注"待人工补全"，不自动填充错误信息 |
| E4 | 任务被删除后，所属业务口进度如何变化 | 【待填写】分子分母是否同步扣减 |
| E5 | 某业务口长期未更新数据 | 【待填写】是否标记"数据过期" |

> E4、E5 留给你补——这是考察 PM 是否想到"闭环"的典型问题。

---

## 11. 技术方案概述

| 项 | 选型 |
|---|---|
| 数据库 | SQLite |
| 部署 | 阿里云 |
| 访问方式 | 网址直接访问 |
| 开发方式 | Cursor 辅助开发 |

> ⚠️ 提示：PM 的核心交付物是**需求文档与决策**，不是代码。使用 Cursor 提升开发效率合理，但不能让"用 AI 写代码"替代"自己写需求文档"。
