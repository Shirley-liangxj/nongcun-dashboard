# PRD 五图：先想什么，再画什么（小白版）

> 先认个错：之前我直接让你"画 5 张图"，但没教你"画之前要想清楚什么"。你是小白，没有分析思维，只给图名当然画不出来。
> 记住一句话：**每张 PRD 图 = 回答产品分析里的一个特定问题。你不是"画图画"，是"先把这个问题想清楚，再把答案画出来"。**

## 一、总地图：5 张图 = 5 个分析角度

| 图 | 它回答的问题 | 本项目从哪下手 |
|---|---|---|
| ① 功能结构图 | 系统要帮用户做哪几类事？ | 把用户故事归并到 M1–M5，再拆二级功能 |
| ② 核心业务流程图 | 用户平时怎么走完一个完整业务？ | 选"月度数据闭环"主场景，按时间走查谁做什么 |
| ③ 数据闭环图 | 数据从哪来、到哪去、怎么转回来？ | 追一条数据全生命周期，证明不是"单边价值陷阱" |
| ④ 页面原型 | 用户打开系统看到哪些界面、怎么跳？ | 功能结构+流程 → 推导需要几个页面、每页放什么 |
| ⑤ 技术架构图 | 用什么技术把上面搭出来？ | 从非功能需求（内网/IE/并发小/安全）推导分层 |

**画的顺序**：① → ② → ③ → ④ → ⑤。前一张是后一张的输入，别一起画。

## 二、逐图拆解（带本项目的例子）

### 图1 功能结构图 = 系统帮用户做哪几类事
**分析角度**：从"用户故事"归并功能。用户故事格式：`作为【角色】想要【功能】以便【价值】`。
**本项目怎么做**：
1. 列角色：领导、业务口负责人、录入员（可能兼）、管理员
2. 把已有的用户故事（需求分析文档里有）列出来
3. 归并到 M1–M5 五个一级模块
4. 给每个模块拆 2–4 个二级功能（这就是图上第二层）

**示例（M1 看板 的二级功能）**：
- 全区总览（一张图看全局）
- 分业务口下钻（点开看某个口）
- 分镇下钻
- 预警清单（滞后的自动标红）
- 同比/环比对比

**你动手填**：把 M2 进度管理、M3 AI分析、M4 数据导入、M5 任务管理 各自拆出二级功能，发我批改。

### 图2 核心业务流程图 = 用户怎么走完一个完整业务
**分析角度**：选一个"最代表系统价值"的主场景，按时间顺序走查：谁、在什么节点、做什么、系统做什么、流向哪。用泳道图（纵向=角色）。
**本项目主场景（月度数据闭环）**，8 个节点：
1. 业务口负责人 从 Excel/手工 录入本月数据（M4）
2. 系统 按主键 upsert 入库，看板自动刷新（M1）
3. 领导 打开看板看全区进度，发现某口滞后
4. 领导 用 AI 分析 看滞后原因（M3 固定报告 / 对话）
5. 领导 下发整改任务给该业务口（M5）
6. 业务口 收到任务，更新进度（M2）
7. 系统 标记闭环，看板更新（M1）
8. 领导 一键导出汇报材料（M3 回血点）

画成泳道：业务口 / 系统 / 领导 三列，箭头串起上面 8 步，第 3 步加个菱形"是否滞后？"。

### 图3 数据闭环图 = 数据从哪来、到哪去、怎么转回来
**分析角度**：追踪"一条数据"的全生命周期，证明系统能自己转起来（不是只有领导单方面用）。
**本项目数据流**：
- 源头：8 业务口 手工 / Excel
- 入口：M4 导入，按"业务口+镇+村+户+年"主键 upsert 去重
- 加工：M1 聚合出看板 + M3 AI 分析
- 消费：领导查看 / 导出汇报（**回血点**：让领导有动力持续用）
- 反馈：M5 任务下发 → 业务口更新 → 数据回流
- 闭环：数据 → 决策 → 行动 → 新数据

### 图4 页面原型 = 用户看到哪些界面、怎么跳
**分析角度**：功能结构图（有什么）+ 业务流程图（怎么用）→ 推导需要哪些页面 → 每页放什么模块。
**本项目页面清单（MVP）**：
- 登录页
- 看板首页（总览卡 + 镇分布 + 预警列表）
- 进度管理页（表格/树，滞后标红）
- AI 分析页（左：固定报告 / 右：对话窗）
- 数据导入页（上传区 + 校验结果）
- 任务页（我收到的 / 我下发的）

小白做法：不用墨刀先画精美，纸上画框图标"这页有什么模块"即可，重点是信息架构对。

### 图5 技术架构图 = 用什么技术搭出来
**分析角度**：从非功能需求推导分层。本项目：内部部署（阿里云）、IE 兼容（网页）、并发小、数据安全（权限/留痕）。
**分层**：
- 展示层：Web 前端（兼容 IE 内核）
- 应用层：后端服务（处理逻辑/AI 调用）
- 数据层：SQLite
- 部署层：阿里云 ECS
- 安全层：登录鉴权 + 角色权限 + 操作留痕

## 三、你现在的第一步
1. 读上面"图1 功能结构图"
2. 把 M2–M5 各自的二级功能列出来（参考 M1 示例的格式）
3. 发我，我帮你批改、纠偏
4. 一张一张来，别贪多

（图2–图5 我会在你画完图1 之后，用同样"先给角度、你画、我批改"的方式带）
