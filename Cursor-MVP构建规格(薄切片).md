# Cursor MVP 构建规格（薄切片）

> **用途**：这是给 Cursor 的构建 prompt / 规格。本文件锁定「范围 / 技术栈 / 假数据 / 约束」，避免 Cursor 自由发挥跑偏。
> **对应 PRD**：M1 数据看板 + M3 AI 双通道（对话式 ChatBI）+ 一键导出汇报。**M4 真实导入 / 整改任务闭环 本期不做。**
> **红线（最高优先级）**：全部使用**假数据（合成台账）**，绝不接入真实政务数据（真实村名 / 真实台账 / 内网地址 / 真实单位名）。违反即返工。

---

## 0. 目标（给领导看什么）

一个能打开的网址，领导看到三件事：

1. **全区各业务口进度一屏看全**，滞后的自动标红、浮到最前；
2. **能像聊天一样问**"XX 镇改厕完成率多少""哪个业务口最滞后"，系统用大白话答，并标注数据来源；
3. **点一下导出**一份汇报 Excel。

→ 证明的产品价值：**少开会、快拿结论**（对应北极星「综合分析获取时延」）。

---

## 1. 技术栈（锁版本，不让 Cursor 自作主张）

| 项 | 选型 | 说明 |
|---|---|---|
| 后端 | Python 3.11 + Flask 3.x | 与你「小云智服」一致，复用经验 |
| 数据库 | SQLite（文件库，seed 假数据） | 零运维，demo 够用 |
| 前端 | 原生 HTML + CSS + 少量 JS | 不引 React/Vue 等重型框架，demo 够用 |
| AI | 智谱 GLM API（走后端代理，key 放 .env） | 你已有免费 key |
| 部署 | PythonAnywhere（复用既有账号） | 直接出公开 URL |

⚠️ **禁止 Cursor 自行引入未列依赖**；如需新增库，必须先告诉你，你确认后再加。

---

## 2. 假数据 Schema（合成，绝无真实村名）

表 `progress`（进度台账）：

| 字段 | 类型 | 示例（假数据，用代号） |
|---|---|---|
| id | INTEGER PK | 1 |
| 业务口 | TEXT | 厕所改造 / 产业奖补 / 高标准农田 / 防止返贫 / 集体经济 |
| 镇 | TEXT | 示范镇A / 示范镇B / 示范镇C（**用代号，不用真镇名**） |
| 年份 | INT | 2023 / 2024 / 2025 |
| 已完成数 | INT | 35 |
| 总数 | INT | 57 |
| 更新时间 | DATE | 2025-08-01 |

**Seed 规模**：5 业务口 × 3 镇 × 3 年 ≈ 45 行。**故意让 2–3 行「完成率 < 时间进度×80%」**以演示预警标红。

---

## 3. 功能清单（MVP 只做这 3 个）

### F1 看板首页（`/`）
- 卡片：每个「业务口 × 镇」一张卡，显示 `已完成/总数` + 进度条 + 完成百分比
- 预警规则：完成率 < (当前年份已过天数 / 365) × 80% → 标「滞后」红底高亮
- 排序：滞后优先 + 完成率升序（让问题自动浮到最前）
- 顶部汇总：总完成率、滞后项数

### F2 AI 对话（ChatBI，`/chat`）
- 输入框 + 对话区；领导用大白话提问
- 后端把**聚合后的假数据**作为 context 发给智谱 GLM，回答自由提问
- **强约束（写进给模型的 system prompt）**：每条回答末尾固定附
  `数据来源：业务口X / 镇Y / 年Z / 数值A,B`，**禁止无来源的编造式回答**
- HITL（人在回路）：回答可标「有误」按钮（MVP 先留修正接口，不必全做）
- **失败降级**：模型不可用 → 返回「暂无法分析，请稍后」，而非页面崩溃

### F3 一键导出（`/export`）
- 点按钮 → 后端用 `openpyxl` 生成 Excel（当前看板视图）→ 浏览器下载
- 表头：业务口 / 镇 / 年份 / 已完成 / 总数 / 完成率 / 状态(正常/滞后)

---

## 4. 约束（写给 Cursor 的铁律）

- **密钥**：GLM key 只在后端 `.env` / 环境变量，严禁写进前端或硬编码源码（用 `python-dotenv` 读）
- **SQL**：所有查询**参数化**，禁止字符串拼接（`cursor.execute("... ?", (val,))`）
- **数据**：只用 seed 假数据；代码里出现任何真实村名 / 真实单位名 / 内网地址 → 返工
- **安全**：前端不做权限控制（demo 全员可看，符合 PRD 透明型）；但密钥与后端接口不外泄
- **环境**：不接真实政务内网、不上真实阿里云生产库（demo 用 PythonAnywhere + 假库）

---

## 5. 明确不做（防范围蔓延）

- ❌ M4 真实 Excel 上传 / Upsert（薄切片不加）
- ❌ 整改任务闭环 / 销号
- ❌ 登录注册 / 权限体系
- ❌ 真实数据同步

---

## 6. 部署到 PythonAnywhere（你自己走的步骤）

1. 本地 `python app.py`，浏览器看效果、自测通过
2. 登录 PythonAnywhere（你已有账号），新建 **Flask web app**
3. 把代码 + seed 推到 PA 的 git 或手动上传
4. 在 PA 控制台 **环境变量** 设 `GLM_API_KEY`
5. 点 **Reload**，拿到 `*.pythonanywhere.com` 网址
6. 把网址发领导

---

## 7. 验收（你自己测）

- [ ] 首页能看到卡片 + **至少 2 个红底「滞后」**
- [ ] 问「哪个业务口最滞后」→ 答出具体业务口 + 镇，且**带数据来源**
- [ ] 点导出 → 下载 Excel，数据与看板一致
- [ ] 断网 / key 无效时，对话区返回友好提示而非白屏

---

> 本规格由 AI PM 导师（g-end-ai-pm + product-manager + 张一鸣视角）起草，作为你用 Cursor 构建的**输入**。代码由你本人写（作品集必须自己来），导师批改。
